# Планируемые фичи

- Добавить propagation состояния приложения по настройкам через разные UI: Qt - WebUI - потенциально какой-нибудь ещё.
  - Сейчас при включении чекбокса Replay seeds before recs в WebUI, в Qt интерфейсе не видно изменений.

- Добавить возможность разных стратегий генераций треков
  - [x] Включать и выключать каждую стратегию можно чекбоксом в отдельном блоке UI
  - Новые стратегии
    - [x] Фильтр на explicit content, в last.fm вроде нет такого, тогда надо смотреть при поиске треков в Spotify
      - Должен быть опциональным. Т.е. также через чекбокс в стратегиях. По умолчанию включен
    - [x] Использовать случайный track.getSimilar по сиду
    - [x] При добавлении рекомендованного трека любой стратегии по сиду, перед добавлением рекомендованного трека в очередь, добавлять сначала сам сид
      - [x] Сделать числовой параметр: один раз на сколько сидов осуществлять такое добавление
      - [x] Такие параметры размещать в одной строке после label чекбокса
    - [x] Добавить persistent history для already queued/generated треков и подмешивать её в `excluded_track_ids`, чтобы генератор не выдавал повторы
    - Добавлять случайный трек из библиотеки аутентифицированного last.fm пользователя.
      - Надо при этом вычислять количество доступных страниц (`<recenttracks user="RJ" page="1" perPage="10" totalPages="3019">`) 
      - Наверное для запросов user.getRecentTracks лучше брать лимит чем меньше тем лучше (проверить), чтобы не грузить сеть и API. В целом perPage="10" норм.
    - Добавлять случайный loved трек из библиотеки пользователя тем же образом
      - Проверить если задавать limit и pages будет ли ответ приходить в таком же виде как recenttracks, если нет, то придумать как можно вычислить totalPages
    - Брать случайный трек из библиотеки пользователя в качестве сида, который будет отдаваться рандомным образом на одну из включенных стратегий принимающих сид (track.getSimilar, artist.getSimilar, ...)
    - Брать случайный loved трек в качестве сида, ...
    - Генерировать трек того же артиста по сиду.
      - Добавить числовой параметр: раз на сколько сидов осуществлять такое добавление.
  - Возможность раздавать веса для рандомного выбора стратегиям
  - Фильтр треков по определённым словам
  - Фильтр треков по списку тегов

- Вынести все api key и secret в .env, можно использовать gem dotenv для работы с ним.

- [x] Добавить ограничение на количество треков в Queue сверху. Превышая этот параметр планировка генерации не должна срабатывать.

- При выборе плейлиста, нужно чтобы он так же, как сейчас, быстро загружался в список Tracks
  - [x] Все недозагруженные треки из него дозагружаются фоном, с небольшой паузой, а не только при скролле списка Tracks.
  - Надо иметь в виду, что в перспективе нужно будет иметь возможность выбирать сразу несколько плейлистов в качестве источника для seeds. Поэтому загрузка по scroll наверное вообще не вариант будет уже, можно даже выпилить, если не очевиден какой-нибудь компромис.

- Надо добавить возможность работы без Qt фронта вообще. Если будет запускаться приложение, скажем, чисто на VPS.

- Возможность банить сиды/треки
  - Перманентно, на время
  - Иметь доступ к списку банов

- Возможность удалять сиды/треки из источников (Spotify, Last.fm)

- Редизайн Qt приложения
  - Circle bg gradient как в WebUI
  - Collapsable blocks
  - Manual alignment
  - Modals для сложного редактирования (например, track title words blacklist)
  - Убрать лишнее
    - Title/Description,
    - Connect/Disconnect сделать в виде активных/неактивных иконок,
    - Search tracks on Spotify
